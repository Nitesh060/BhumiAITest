# Core web app (Backend/app.py and everything it imports) — the only
# things the deployed Render service actually needs. Kept lean on purpose:
# torch, pandas, scikit-learn, opencv-python-headless, google-genai,
# matplotlib, Pillow and a top-level numpy pin were removed because
# nothing in the running app imports them (grep-confirmed) — they were
# only bloating build time/size on Render's free tier and had nothing to
# do with the actual OOM crash. torch/numpy/Pillow are still used by the
# offline training scripts (train_land_cover.py, generate_synthetic_dataset.py)
# — install those two locally / in a separate training environment if you
# run those scripts, they don't belong in the deployed web service.
Flask
flask-cors
earthengine-api
python-dotenv
requests
gunicorn
reportlab
SQLAlchemy
psycopg2-binary
PyJWT
pytest
